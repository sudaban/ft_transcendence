Anlık hazır olan kısımlar 

-------------------------
Frontend (SvelteKit + Tailwind)

Node 20 Alpine multi-stage Dockerfile
npm run build → SvelteKit production build
node build/index.js → 3000 portunda çalışır
Tailwind CSS + Forms plugin entegre
Basit landing page örneği (/src/routes/)

Backend (.NET 8)

ASP.NET Core 8.0 Alpine multi-stage build
PostgreSQL bağlantısı (appsettings.json)
CORS aktif
5000 portunda çalışır

Nginx Reverse Proxy

Port 8080 dış erişim
WebSocket desteği aktif:
Connection: upgrade header
Upgrade header

Routing:
/ → Frontend (3000)
/api/* → Backend (5000)
/ws → Backend WebSocket

Erişim Noktaları
http://localhost:8080/        → Frontend
http://localhost:8080/api/    → Backend API
localhost:5432                → Database

docker-compose up -d
make logs              # Logları gör
make shell-backend     # Backend container'a gir

-------
version 2.0
-------

şuan temel oalrak build alıp çalıştırabiliyoruz sistemi fakat database yada farklı sayfalarda
end point vs ayarlanmadığı iççin sağlıklı bir performans yok
prometheus ve grafanayıda kurduk 2 deneme yaptım 2sinde de  çalıştı  
--------

grafana containerına localhost:3000 yerine localhost:3001 yaptım
3000 olması gerekiyor galiba

sudo service docker start   ile docker başlat wsl de bazen daemon kapalı oluyor ve yetkisi olmuyor

port bağlantılarında çakışmalar olabilir kontrol edilmesi gerekiyor


wsl ile çalıştığında çakışma olabiliyor: (docker ps çıktısında OS: Linux 6.18.33.2-microsoft-standard-WSL2 görünüyor)

WSL2 içindeki localhost doğrudan Windows tarayıcısına yansımayabilir.  

tarafında 80 veya 443 portunu başka bir uygulama (IIS, Skype vs.) kullanıyor olabilir. Windows PowerShell'de netstat -ano | findstr :443 ile kontrol edebilirsiniz.

package-lock.json kopyalanması	Versiyon çözümleme isteklerini tamamen ortadan kaldırır
--no-audit	npm audit HTTP isteklerini atlar (güvenlik taraması)
--no-fund	Funding bilgisi isteklerini atlar
--prefer-offline	Önce local cache'e bakar, bulamazsa internete gider
--maxsockets=5	Paralel bağlantıları sınırlar (WSL2 DNS'inin boğulmasını engeller)

Container → Docker Bridge → WSL2 VM NAT → Windows Host → İnternet
Bu her HTTP isteğine ekstra ~50-100ms latency ekliyor. 200 paket × 50ms = +10 saniye sadece latency'den. Paket çözümleme istekleri ile bu katlanıyor.

---------------------------------------------------------------------
---------------------------------------------------------------------

 Prometheus + Grafana (Sayılar ve Sağlık Durumu)
Bu ikili sistemin nabzını ölçer. Sadece sayısal verilerle (metriklerle) ilgilenir.

Ne toplar? "Şu an CPU yüzde kaç?", "RAM ne kadar doldu?", "Saniyede kaç kişi siteye giriyor?", "Veritabanında kaç aktif bağlantı var?" gibi istatistikleri toplar.
Grafana ne yapar? Prometheus'un topladığı bu sıkıcı sayıları alır; çok şık, anlık değişen renkli grafiklere, hız göstergelerine (dashboard) dönüştürür.
Hangi soruyu cevaplar? "Sistem şu an sağlıklı mı? Sunucu yavaşlıyor mu? Çökmek üzere mi?"
2. ELK Stack (Metinler, Hata Kayıtları ve Dedektiflik)
ELK (Elasticsearch, Logstash, Kibana) ise sistemin günlüğünü (kara kutusunu) tutar. Sadece metin tabanlı log kayıtlarıyla ilgilenir.

Ne toplar? Senin Backend veya Frontend kodunda konsola yazdırdığın tüm çıktıları (Console.WriteLine, console.log), sunucunun verdiği spesifik hata mesajlarını (örneğin: "Kullanıcı X şifresini yanlış girdi", "Satır 42'de NullReferenceException hatası oluştu").
ELK Nasıl Çalışır?
(L)ogstash / Filebeat: Bütün konteynerleri dinler ve onların konsol çıktılarını (loglarını) anlık olarak toplar.
(E)lasticsearch: Bu toplanan milyonlarca satır log metnini kendi devasa veritabanına kaydeder ve saniyeler içinde arama yapılabilecek şekilde (Google gibi) indeksler.
(K)ibana: Senin bu Elasticsearch içindeki milyonlarca log metni arasında filtreleme, arama yapmanı ve hataları bulmanı sağlayan arayüzdür.
Hangi soruyu cevaplar? "Sistem neden yavaşladı? Hangi kod satırında hata fırlatıldı? Saat 14:05'te sisteme kim giriş yapmaya çalıştı?"


make monitoring-up	+ Monitoring	11 (~2 GB RAM)
make full-up	Hepsi	15 (~4-5 GB RAM)